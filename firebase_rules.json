{
    "rules": {
        "stats": {
            ".read": true,
            ".write": true,
            "totalGames": {
                ".validate": "newData.isNumber()"
            },
            "abbasWins": {
                ".validate": "newData.isNumber()"
            },
            "nasirWins": {
                ".validate": "newData.isNumber()"
            }
        }
    }
}